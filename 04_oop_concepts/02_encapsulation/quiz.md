# 🔒 Encapsulation Quiz

## 1. What is the main goal of encapsulation?
- [ ] To make code run faster
- [ ] To bind data and methods together and hide internal details
- [ ] To create multiple copies of an object
- [ ] To inherit behavior from other classes

## 2. How do you define a "private" attribute in Python?
- [ ] Use the `private` keyword
- [ ] Start the variable name with one underscore `_var`
- [ ] Start the variable name with two underscores `__var`
- [ ] Capitalize the variable name `VAR`

## 3. What does the single underscore prefix (e.g., `_variable`) indicate?
- [ ] It is strictly private and cannot be accessed
- [ ] It is a convention meaning "internal use only" or "protected"
- [ ] It is a global variable
- [ ] It is a system variable

## 4. What is a "getter" method used for?
- [ ] To set the value of a private variable
- [ ] To retrieve the value of a private variable
- [ ] To delete a variable
- [ ] To print all variables

## 5. Which decorator creates a "getter" property in Python?
- [ ] `@classmethod`
- [ ] `@staticmethod`
- [ ] `@getter`
- [ ] `@property`

---

### Answers
1. To bind data and methods together and hide internal details
2. Start the variable name with two underscores `__var`
3. It is a convention meaning "internal use only" or "protected"
4. To retrieve the value of a private variable
5. `@property`
