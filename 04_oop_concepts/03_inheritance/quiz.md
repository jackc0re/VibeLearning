# 👨‍👦 Inheritance Quiz

## 1. What applies to the `Child` class in `class Child(Parent):`?
- [ ] It creates a new copy of the Parent class
- [ ] It inherits attributes and methods from Parent
- [ ] It deletes the Parent class
- [ ] It makes the Parent class private

## 2. In Python, which function is used to access methods from the parent class?
- [ ] `parent()`
- [ ] `base()`
- [ ] `super()`
- [ ] `self()`

## 3. What is "Method Overriding"?
- [ ] When a child class provides a specific implementation of a method already defined in its parent class
- [ ] When a method calls itself
- [ ] When two methods have the same name in the same class
- [ ] When you delete a method

## 4. What does `isinstance(obj, MyClass)` check?
- [ ] If `obj` is exactly an instance of `MyClass` (excluding subclasses)
- [ ] If `obj` is an instance of `MyClass` or any of its subclasses
- [ ] If `obj` has the same name as `MyClass`
- [ ] If `obj` is defined in the same file as `MyClass`

## 5. Can a class inherit from multiple classes in Python?
- [ ] Yes
- [ ] No
- [ ] Only if they are in different files
- [ ] Only if the classes are empty

---

### Answers
1. It inherits attributes and methods from Parent
2. `super()`
3. When a child class provides a specific implementation of a method already defined in its parent class
4. If `obj` is an instance of `MyClass` or any of its subclasses
5. Yes
