# 🎭 Polymorphism Quiz

## 1. What does Polymorphism mean in programming?
- [ ] Many shapes
- [ ] Multiple inheritance
- [ ] The ability of different objects to respond to the same method call
- [ ] Converting data types

## 2. What is "Duck Typing"?
- [ ] A type checking system that enforces strict types
- [ ] A style where an object's suitability is determined by the presence of certain methods/attributes, rather than its type
- [ ] Typing with only two fingers
- [ ] A way to inherit from ducks

## 3. Which magic method is used to define the string representation of an object (for `print()`)?
- [ ] `__init__`
- [ ] `__repr__`
- [ ] `__str__`
- [ ] `__string__`

## 4. Which magic method is used to overload the `+` operator?
- [ ] `__plus__`
- [ ] `__add__`
- [ ] `__sum__`
- [ ] `__append__`

## 5. If `a` is an object of class `A` and `b` is an object of class `B`, and both classes have a method `foo()`, is `a.foo()` and `b.foo()` an example of polymorphism?
- [ ] Yes
- [ ] No, they must be the same class
- [ ] Only if they inherit from the same parent
- [ ] Only if the methods return the same value

---

### Answers
1. The ability of different objects to respond to the same method call
2. A style where an object's suitability is determined by the presence of certain methods/attributes, rather than its type
3. `__str__`
4. `__add__`
5. Yes
