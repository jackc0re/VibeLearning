# 🌫️ Abstraction Quiz

## 1. Can you create an instance of an Abstract Base Class (ABC)?
- [ ] Yes, always
- [ ] No, never
- [ ] Only if it has no methods
- [ ] Only if you use `super()`

## 2. Which module do you need to import to create an Abstract Base Class in Python?
- [ ] `abstract`
- [ ] `abc`
- [ ] `interface`
- [ ] `base`

## 3. What decorator is used to declare an abstract method?
- [ ] `@abstractmethod`
- [ ] `@static`
- [ ] `@virtual`
- [ ] `@override`

## 4. What happens if a child class fails to implement an abstract method from its parent ABC?
- [ ] The code runs but the method does nothing
- [ ] The child class executes the parent's empty method
- [ ] Python throws an error when you try to instantiate the child class
- [ ] Python automatically generates the method

## 5. What is the main purpose of Abstraction?
- [ ] To write more lines of code
- [ ] To hide complex implementation details and expose only what is necessary
- [ ] To make code run faster
- [ ] To prevent other programmers from reading your code

---

### Answers
1. No, never
2. `abc`
3. `@abstractmethod`
4. Python throws an error when you try to instantiate the child class
5. To hide complex implementation details and expose only what is necessary
